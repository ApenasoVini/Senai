CREATE TABLE funcionario (
    id_funcionario_pk VARCHAR(20) NOT NULL,
    nome VARCHAR(30) NOT NULL,
    salario INT NOT NULL,
    data_admissao DATE NOT NULL,
    PRIMARY KEY (id_funcionario_pk)
);

CREATE TABLE departamento (
    id_departamento_pk VARCHAR(20) NOT NULL,
    nome VARCHAR(30) NOT NULL,
    localizacao VARCHAR(30) NOT NULL,
    PRIMARY KEY (id_departamento_pk)
);

CREATE TABLE projeto (
    id_projeto_pk VARCHAR(20) NOT NULL,
    nome VARCHAR(30) NOT NULL,
    data_inicio DATE NOT NULL,
    data_conclusao DATE NOT NULL,
    PRIMARY KEY (id_projeto_pk)
);

CREATE TABLE habilidade (
    id_habilidade_pk VARCHAR(20) NOT NULL,
    descricao VARCHAR(30) NOT NULL,
    PRIMARY KEY (id_habilidade_pk)
);

CREATE TABLE funcionario_departamento (
    id_funcionario_fk VARCHAR(20) NOT NULL,
    id_departamento_fk VARCHAR(20) NOT NULL,
    FOREIGN KEY (id_funcionario_fk) REFERENCES funcionario (id_funcionario_pk),
    FOREIGN KEY (id_departamento_fk) REFERENCES departamento (id_departamento_pk)
);

CREATE TABLE projeto_departamento(
    id_departamento_fk VARCHAR(20) NOT NULL,
    id_projeto_fk VARCHAR(20) NOT NULL,
    FOREIGN KEY (id_projeto_fk) REFERENCES projeto (id_projeto_pk),
    FOREIGN KEY (id_departamento_fk) REFERENCES departamento (id_departamento_pk)
);

CREATE TABLE funcionario_habilidade(
    id_funcionario_fk VARCHAR(20) NOT NULL,
    id_habilidade_fk VARCHAR(20) NOT NULL,
    FOREIGN KEY (id_funcionario_fk) REFERENCES funcionario (id_funcionario_pk),
    FOREIGN KEY (id_habilidade_fk) REFERENCES habilidade (id_habilidade_pk)
)

INSERT INTO funcionario (id_funcionario_pk, nome, salario, data_admissao)
VALUES ('1', 'João Silva', 5000, '2022-01-15');
VALUES ('2', 'Maria Santos', 6000, '2021-09-30');

INSERT INTO departamento (id_departamento_pk, nome, localizacao)
VALUES ('A', 'RH', 'São Paulo');
VALUES ('B', 'Financeiro', 'Rio de Janeiro');

INSERT INTO projeto (id_projeto_pk, nome, data_inicio, data_conclusao)
VALUES ('P1', 'Projeto A', '2022-03-01', '2022-06-30');
VALUES ('P2', 'Projeto B', '2023-01-15', '2023-08-31');

INSERT INTO habilidade (id_habilidade_pk, descricao)
VALUES ('H1', 'Programação');
VALUES ('H2', 'Gestão de Pessoas');

INSERT INTO funcionario_departamento (id_funcionario_fk, id_departamento_fk)
VALUES ('1', 'A');
VALUES ('2', 'B');

INSERT INTO projeto_departamento (id_projeto_fk, id_departamento_fk)
VALUES ('P1', 'A');
VALUES ('P2', 'B');

INSERT INTO funcionario_habilidade (id_funcionario_fk, id_habilidade_fk)
VALUES ('1', 'H1');
VALUES ('2', 'H2');

SELECT * FROM funcionario
SELECT * FROM departamento
SELECT * FROM projeto
SELECT * FROM habilidade
SELECT * FROM funcionario_departamento
SELECT * FROM projeto_departamento
SELECT * FROM funcionario_habilidade